# Build output
target
dist
node_modules
.angular

# Engine data directories.
# Both spellings appear in the codebase: `src/bin/server.rs` defaults to
# `./.lsm_data`, while `CoreConfig::default()` uses `./.lsmdata`.
.lsm_data
.lsmdata

# Local scratch space
/local
.notes/

# Secrets -- never commit these
.env
.secrets
credentials
*.pem
*.key
!scripts/*.sh

# Tool-local config
.cargo/config.toml
.aider*

# Agent scratch state (regenerated per run, not source of truth)
.task-state.json
.apexstore/
